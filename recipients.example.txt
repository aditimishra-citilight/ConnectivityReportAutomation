# Who receives the Connectivity Report.
#
# Copy this file to  recipients.txt  and put the real addresses in it.
# recipients.txt is gitignored so nobody's address ends up on GitHub.
#
# One email address per line. Lines starting with # are ignored, so you can
# comment someone out instead of deleting them. Blank lines are fine.
#
# Changes take effect on the NEXT run — nothing to restart, no code to touch.
# To check what the system will actually use:  node show-recipients.js
#
# For CC recipients, make a second file called  recipients-cc.txt  in the
# same format.

someone@yourdomain.com
someone.else@yourdomain.com
